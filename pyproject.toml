[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v"

[tool.pytest.ini_options.markers]
integration = "Requires a live ClickHouse instance (use --integration)"
